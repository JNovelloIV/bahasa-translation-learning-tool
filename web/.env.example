# Production only. The absolute URL of your deployed Worker, e.g.
#   VITE_API_BASE=https://sehari-worker.<your-subdomain>.workers.dev
# In local dev this is unset and the Vite /api proxy talks to wrangler on :8787.
VITE_API_BASE=
